#include "hello_world.h"
#include "print.h"
#include "string_change.h"
#include <cstring>

int main() {
    std::string jopa("Jopa");
    // char* str = strdup("pohuy");
    // unsigned int len = strlen(str);
    // SwapBytes(str, len);
    // Print(str);
    // print(str, len, "lol");
    // free(str);
}
